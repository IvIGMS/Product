spring:
  application:
    name: Products

  datasource:
    url: jdbc:postgresql://localhost:5433/products-categories
    username: user
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8083
